# PathPilot backend – copy to .env and fill in
# Required for real AI (career guidance, resume improve, cover letters):
OPENAI_API_KEY=sk-your-openai-key-here
# Optional: custom OpenAI base URL (e.g. Azure or proxy)
# OPENAI_BASE_URL=https://your-endpoint.openai.azure.com/
# Optional: model (default gpt-4o-mini)
# LLM_MODEL=gpt-4o-mini

# Job search: SerpAPI first (Google Jobs), then Adzuna (free), then mock so frontend always has jobs
# SERPAPI_KEY=your-serpapi-key
# Adzuna (free): https://developer.adzuna.com/signup — use as fallback when SerpAPI fails or returns empty
# ADZUNA_APP_ID=your-app-id
# ADZUNA_APP_KEY=your-app-key

# RapidAPI Indeed (additional source): https://rapidapi.com — indeed12.p.rapidapi.com company jobs
# RAPIDAPI_KEY=your-rapidapi-key

# Optional: app secret and DB
# SECRET_KEY=change-me-in-production
# DATABASE_URL=sqlite:///./pathpilot.db

# CORS: frontend origins are always allowed (localhost:3000, 127.0.0.1:3000). For production, add:
# CORS_ORIGINS=https://your-frontend.vercel.app,https://www.yourdomain.com
